# text-sentiment-classification
This project implements a supervised machine learning model for binary sentiment classification using a small manually labeled dataset of short text reviews. It demonstrates text preprocessing, feature extraction, model training, and evaluation within a complete and structured ML pipeline.
